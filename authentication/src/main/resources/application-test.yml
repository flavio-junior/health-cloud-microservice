spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_auth0_spring_boot_and_kotlin?useTimezone=true&serverTimezone=${TIME_ZONE}
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    show-sql: false